#!/usr/bin/env bash
# bin/release <build-dir>

echo "Release invoked."

cat <<EOF
addons: []
config_vars: []
default_process_types:
  web: java -jar jenkins.war -Djenkins.install.runSetupWizard=false
EOF
