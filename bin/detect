#!/usr/bin/env bash
# bin/detect <build-dir>

build_dir=$1
jenkinswarfile_path=$build_dir/jenkins.war 

if [ -f jenkinswarfile_path ]; then
  echo jenkinswarfile_path && exit 0
else
  echo "no" && exit 1
fi
